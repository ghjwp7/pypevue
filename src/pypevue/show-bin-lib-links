#!/bin/sh

# Called without an argument: Make fresh link in

LLIB=$(python3 -c "import sys; print ([x for x in sys.path if '/home/' in x][0])")
LBIN=$HOME/.local/bin

ls -al  $LBIN/pype*  $LBIN/geo*
ls -al  $LLIB/pype*  $LLIB/geo*
